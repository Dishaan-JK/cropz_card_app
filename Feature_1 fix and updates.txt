You are an expert Flutter Developer. Refactor and update the "Cropz Card" application by executing the following list of fixes and feature additions. Group your changes logically to ensure app state and UI rendering do not break.

[SECTION 1: UI FIXES & LAYOUT ADJUSTMENTS]

1. Main Screen & Directory:
   - Locate and completely remove the "Universal Directory x number of cards available" widget situated directly below the search bar.
   - Modify the bottom-right "Create Card" widget: It must be hidden/removed if the user currently has zero cards registered.
   - Update the card click behavior: When a user taps a card, open its full preview view. Remove any smaller "pre-view" widgets that currently exist inside the collapsed card layout.
2. Navigation & Spacing (Apply globally where applicable):
   - Reposition the "Log Out" option higher on the screen so it does not blend with or get covered by the mobile operating system's bottom navigation bar.
   - Shift the "Cropz Hub" and "Elements" widgets slightly lower to prevent them from blending with the mobile OS top status bar.
   - Relocate the "Add Accounts" and "Log Out Accounts" buttons from their current position directly into the "Settings" page.
3. Authentication & Log-In Screen:
   - Remove the "Cropz Log-in" title and all its associated content.
   - Replace it with a new header: "Welcome to Cropz Card" and include a small temporary string acting as an app description.
   - Remove the "Tap the button to log-in" widget completely.

[SECTION 2: LOADING & LOGIC FIXES]

1. Post-OTP Verification State:
   - Fix the brief flickering of the Log-in screen that occurs after successful OTP verification while the app is loading.
   - Implementation: Remove that intermittent state and route the user to a dedicated, clean Loading screen instead.
2. White-Labeling & Copy Removal:
   - Scrub and remove all instances of the third-party OTP provider name "Phone.email" and the sentence "Verified by Phone Email" across the entire application's source code.

[SECTION 3: NEW FEATURE UPDATES]

1. Theme Defaults:
   - Change the application's default theme mode from dark mode to Light Mode.
2. Micro-Interactions:
   - Add a smooth background animation that triggers or plays subtly whenever widgets are clicked or viewed on screen.
3. External OTP SMS Configuration (Mock / Placeholder):
   - Provide a configuration block or localized strings reflecting that the OTP sender name displayed in the SMS/Messages app is "Cropz_digi_services".
   - Ensure that instances labeled "JSL" are updated to say "Cropz Card".

Constraints:
- Do not remove functional login operations; only clean up the visual layouts and loading states specified.
- Maintain the offline-first sync architecture if these widgets read local SQLite data.
-Do not show the name of the otp auth provider.
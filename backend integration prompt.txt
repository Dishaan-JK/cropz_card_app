You are an expert Flutter Developer and Supabase Architect. 
I want to integrate my application, "Cropz Card", with Supabase to handle multi-device session management, cross-device data recovery, and hardware telemetry. 

Context & Tech Stack:
- App: Flutter (supporting an offline-first workflow via local SQLite).
- Backend: Supabase (handling auth, table storage, and cloud syncing).

Functional Requirements:

1. Single Device Session Enforcement (Forced Logout):
   - When a user logs into Device B with the same account, the session on Device A should automatically be revoked or forced to log out.
   - Requirement: Detail the database schema (e.g., storing a unique 'active_session_id' in a user_sessions table) and the Row Level Security (RLS) policies needed in Supabase to block Device A once its session is no longer the active one.
   - Requirement: Provide a snippet of Flutter/Dart code showing how the app can listen for this auth-state change or catch the RLS error to force the UI back to the login screen on Device A.

2. Cloud-to-Local Sync (Data Recovery for Lost Devices):
   - If a user loses their old device and logs into a new one, all their previously created cards and profile data must be seamlessly pulled down from Supabase and populated into the new device's local SQLite database.
   - Requirement: Explain the synchronization strategy. Outline a Supabase schema for a 'cards' table and provide a high-level Dart repository pattern that checks if the local SQLite database is empty on login, triggering a full pull from the Supabase server.

3. Device Telemetry & Tracking:
   - I want to capture and store the hardware details of the device the user is logging in from (specifically Device Type/Platform and Model).
   - Requirement: Create a Supabase table schema for 'user_devices' linked to the auth.uid().
   - Requirement: Write a short Flutter implementation showing how to use the 'device_info_plus' package to grab the hardware model and push it to Supabase upon successful authentication.

Constraints:
- All designs must honor the app's existing 'offline-first' supremacy where data is written locally first and then synced to Supabase when a connection is established.
- Keep security at the forefront; avoid exposing master service keys to the client side.